version: "3.8"

services:
  db:
    image: mysql:5.7
    volumes:
      - db-data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_DATABASE: myapp_development
      MYSQL_USER: myapp
      MYSQL_PASSWORD: myapp_password
    ports:
      - "3306:3306"

  backend:
    build: .
    command: bash -c "rm -f tmp/pids/server.pid && bundle exec rails s -p 3000 -b '0.0.0.0'"
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    depends_on:
      - db
    environment:
      RAILS_ENV: development
      DATABASE_URL: mysql2://myapp:myapp_password@db/myapp_development

  # frontend:
  #   build: .
  #   command: bash -c "npm install --arch=x64 --platform=linux esbuild && cd frontend && npm run dev"
  #   volumes:
  #     - .:/app
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     - backend

volumes:
  db-data:
